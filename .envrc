# direnv configuration — loaded automatically when entering this directory.
# Requires direnv: https://direnv.net/

dotenv_if_exists .env

# Set project home so test fixtures can locate test resources.
# Used by BaseTestCase and several GraalVM/CommonJS module tests.
export JOSM_SCRIPTING_PLUGIN_HOME="$(pwd)"
